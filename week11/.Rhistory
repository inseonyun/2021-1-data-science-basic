#데이터 분석 시작2(대푯값)
?woman
#데이터 분석 시작2(대푯값)
?women
?iris
?mtcars
?airquality
?diamond
?diamonds
women
class(women)
iris
class(iris)
mtcars
iris
airquality
srt(women)
str(women)
women.$height
women$height
women%&weight
women%weight
women$weight
str(iris)
class(mtcars)
str(mtcars)
class(airquality)
?Titanic
Titanic
class(Titanic)
str(Titanic)
class(airquality)
str(airquality)
#외부 패키지 가져오기(기존 패키지에 다이아 없음)
#외부 패키지 설치하는법 -> install.packages
install.packages('ggplot2')
?diamonds
library(ggplot2)
?diamonds
class(didamos)
class(diamos)
class(diamonds)
diamonds
str(diamonds)
head(iris)
tail(iris)
names(iris)
dim(iris)
ncol(iris)
nrow(iris)
table(iris)
table(iris$Species)
summary(iris)
#내 시스템에 어떠한 데이터가 있는지 알려주는 함수
data()
women[2,2]
# 여러 개의 데이터 가져오기
women[c(2,3),2]
# 여러 개의 데이터 가져오기
women[c(2:3),2]
# 여러 개의 데이터 가져오기
women[c(2,3),2]
#1열을 가져온다
women[1,]
#1행을 가져온다
women[,1]
#1행을 가져온다
class(women[1,])                  # dataframe
women$height
women[['height']]
women[[1]]
women[,c('height','weight')] #dataframe으로 가져옴옴
women[[c('height','weight')]] #err남남
women[[1]]
a = 3
a
iris
iris = iris
iris
women[1,1] = 0
women
#global environment에도 women이 있고 패키지에 women에 있을 때, global에 있는 women 지우는 법
rm(women)
women
#만약 women이란 데이터가 필요하고 수정해야한다면?
women.df = women
ls()
class(women)
str(women)  #obs는 관측치, variables는 두개의 변수
Titanic
class(Titanic)
class(diamonds) #다이아를 3개의 컨테이너에서 지원함
ggplot2 # 다이아가 들어있는 패키지 (함수 + 데이터셋)
class(diamonds) #다이아를 3개의 컨테이너에서 지원함
?diamonds
library(ggplot2)
class(diamonds) #다이아를 3개의 컨테이너에서 지원함
str(Titanic)
class(diamonds) #다이아를 3개의 컨테이너에서 지원함
library(ggplot2)
class(diamonds) #다이아를 3개의 컨테이너에서 지원함
head(iris) # 상위 데이터 6개만 가져옴
tail(iris) # 하위 데이터 6개만 가져옴
names(iris) # iris 패키지의 데이터 열 네임 가져옴
dim(iris) # 데이터의 차원을 가져옴( 150행 5열)
ncol(iris)
nrow(iris)
names(women)
dim(iris) # 데이터의 차원을 가져옴( 150행 5열)
ncol(iris)
nrow(iris)
table(iris)
table(iris$Species) #iris에 있는 speices를 테이블로 가져옴
summary(iris) #데이터의 요약 정보를 볼 수 있는 함수
iris
table(iris$Species) #iris에 있는 speices를 테이블로 가져옴
summary(iris) #데이터의 요약 정보를 볼 수 있는 함수
women[[1]]
women[['height']]
#변수명을 이용해서 데이터 가져오기
women$height
?women[[1]] #데이터를 가져옴
class(women[[1]]) #데이터를 가져옴
class(women[1]) #데이터프레임 그대로 유지하며 가져옴
# 패키지에 있는 women을 가져와 1,1 인덱스의 값을 0으로 치환하고, 그 데이터를 global environment에 저장함
women[1,1] = 0 #데이터가 깨지는 행위 ->위험함
women
#만약 women이란 데이터가 필요하고 수정해야한다면?
women.df = women
#만약 women이란 데이터가 필요하고 수정해야한다면?
women.df1 = women
#glbal environment에 있는 변수들 모두 출력
ls()
# 패키지에 있는 women을 가져와 1,1 인덱스의 값을 0으로 치환하고, 그 데이터를 global environment에 저장함
women[1,1] = 0 #데이터가 깨지는 행위 ->위험함
iris = iris
#glbal environment에 있는 변수들 모두 출력
ls()
